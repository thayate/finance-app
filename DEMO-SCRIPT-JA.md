# GitHub SDLC Automation with IBM Bob — デモ台本

> **対象:** IBM Technical Sales（Level 3 Stand & Deliver 用）
> **想定時間:** 約20〜30分
> **架空クライアント:** 金融系SIer 開発チームリード

---

## 評価基準（Stand & Deliver チェックリスト）

マネージャーは以下の3点を見ます。録画前に確認してください。

- [ ] **① お客様の課題と IBM Bob で解決できる理由を言葉にしている**
- [ ] **② IBM Bob のユースケースを具体的に見せている**
- [ ] **③ 次のアクションでデモを締めている**

---

## デモの流れ

```
オープニング（課題・価値提案）
    ↓
Part 1: Build（アプリを設計・構築する）
  Step A — リポジトリ分析と実装計画の生成
  Step B — アプリのスキャフォールディング（サブタスク承認）
  Step C — Yahoo Finance データ層の実装
  Step D — ダッシュボードの構築
  Step E — テスト・バリデーション
  Step F — コミット・PR説明文の生成
    ↓
Part 2: Evolve（GitHubで継続進化させる）
  Step G — GitHubリポジトリへの公開
  Step H — GitHub Issue から機能要件を分析
  Step I — 機能実装
  Step J — バリデーション
  Step K — PRの準備・マージ
    ↓
クロージング（CTA）
```

---

## オープニング（Stand & Deliver 用）

> 📢 **ここから録画を開始してください**

---

### ① お客様の課題

```
【話す内容】

「皆さんのチームでは、こういった経験はないでしょうか。

  GitHub の Issue に要件が書いてあるのに、
  どのファイルを変えればいいか把握するだけで時間を取られる。

  PR 説明文を書くのが面倒で後回しになる。

  テストを書く時間がなくて、CI が壊れてから気づく。

  IDE とブラウザとターミナルを行き来するだけで
  午前中が終わっている。

コードを書いていない時間のほうが長い——これが多くの開発チームの実態です。
IBM Bob はそこに入ります。」
```

---

### IBM Bob で何が変わるか

```
【話す内容】

「コード補完ツールとは違います。

Bob はリポジトリを読み、Issue の内容を理解し、
コードを書いて、テストを実行し、PR の説明文まで用意します。
エンジニアがやるのは、計画を確認して『Go』と言うことだけです。

今日は空のリポジトリから始めて、金融ダッシュボードを作り、
GitHub Issue を起点に機能追加してマージするところまでお見せします。」
```

---

### ② デモの流れ

```
【画面: デモ概要スライドを表示】

「2つのパートに分かれています。

Part 1 は Build です。
空のリポジトリから、Bob が計画を立てて React アプリを作ります。
Yahoo Finance のデータを取ってきて、ダッシュボードを3つ組んで、
テストまで通します。

Part 2 は Evolve です。
GitHub に公開して、Issue を1つ作ります。
Bob が Issue を読んで受け入れ基準を出して、
機能を実装して、PR を準備します。
マージするのは自分です。

では始めます。」
```

---

## 事前確認（録画前に済ませておくこと）

- [ ] Bob IDE が起動し、Agentic Sidebar が表示されている
- [ ] `Github SDLC Automation` フォルダが Bob IDE で開かれている（`finance-app/` は空）
- [ ] Node.js・Git がインストール済み（`node -v` / `git --version` で確認）
- [ ] GitHub.com アカウントにログイン済み
- [ ] SSH で `github.ibm.com` に接続できる（`ssh -T git@github.ibm.com` で確認）

---

## Part 1 | Build

---

### Step A — リポジトリ分析と実装計画の生成

**【話す内容】**
```
「最初に Bob にリポジトリを分析させ、
何を作るべきかを理解させます。
コードを書く前に計画を立てる——これが Bob の流儀です。」
```

**【操作】** Agent モードで以下のプロンプトを貼り付けて Enter

> **💬 プロンプトを貼り付ける前に話す内容**
> 「Bob に渡すのは『このリポジトリを起点に、IBM と競合他社の株価ダッシュボードを作りたい』という一文の依頼です。ただし、それだけだと雑すぎるので、どんなアーキテクチャにすべきか・どうテストするか・GitHub とどう連携するかまで、あらかじめ設計の観点を指定しています。コードは1行も書いていません。まず計画を出させるのが最初のステップです。」

```
このリポジトリを、新しい React ベースの金融分析ラボの出発点として分析してください。
IBM と最大4社の競合他社を Yahoo Finance データで追跡する
マーケットダッシュボードアプリの実装計画を提案してください。

アプリは3つの時間軸ビューを提供します：当日・過去7日間・前四半期。

以下の観点で実用的なアーキテクチャを推奨してください：
- ソースフォルダ構成
- React コンポーネントの境界
- データサービスの抽象化
- チャートライブラリのアプローチ
- 状態管理戦略
- エラーハンドリング
- テスト戦略
- ローカル検証手順

また、GitHub の Issue や Pull Request を通じた
継続的な機能進化をどのように実装でサポートするかも説明してください。
```

**【確認ポイント】**
- Bob が HTML レポートとして実装計画を生成する
- **Open** をクリックしてレポートをプレビュー
- コンポーネント境界・データサービス設計が含まれていることを確認

```
【話す内容】
「ご覧のように、Bob はアーキテクチャの決定を
すべて計画書に落とし込みました。
承認すれば、ここから実装に進みます。」
```

---

### Step B — アプリのスキャフォールディング

**【話す内容】**
```
「計画が合意できたので、スキャフォールドを始めます。
Bob はサブタスクに分解して、1つずつ承認を求めながら進めます。」
```

**【操作】** 同じ Agent セッションで続けて貼り付けて Enter

> **💬 プロンプトを貼り付ける前に話す内容**
> 「計画書が出ました。次のプロンプトは短めです。『React アプリの骨格を作って、サービス層もフォルダ構成も含めてください』という一言です。ここで細かく指定しすぎないのがポイントで、さっき合意した計画書を Bob がすでに読んでいるので、その設計方針に沿って自分でサブタスクに分解して進めます。」

```
このリポジトリに、シンプルな React アプリケーション構造を作成してください。
ダッシュボードページ、再利用可能なチャートカードコンポーネント、
金融データサービス層、そして将来の機能拡張に適した
クリーンなフォルダ構成を含めてください。
```

**【操作・確認】**
- Bob が Todo リストを提示したら **Approve todo tools for task** をクリック
- 各サブタスク（Sub-Task 1〜11）で「Yes, 進めてください」と承認していく

| サブタスク | 内容 |
|---|---|
| Sub-Task 1 | Vite + React 18 + TypeScript プロジェクト初期化 |
| Sub-Task 2 | 定数・型・モックデータの作成 |
| Sub-Task 3 | データサービス層の実装 |
| Sub-Task 4 | カスタムフック（useQuotes / useHistory） |
| Sub-Task 5〜7 | レイアウト・フィードバック・カードコンポーネント |
| Sub-Task 8〜9 | チャート・ビューコンポーネント |
| Sub-Task 10 | App シェル・テスト・設定ファイル |
| Sub-Task 11 | lint + test + build の検証ゲート |

```
【話す内容（Sub-Task 承認中）】
「Bob は依存関係の順序を考慮してバッチ単位で実装しています。
私たちは計画を確認して承認するだけ——
コードを書いているのは Bob です。」
```

**【操作】** 全タスク完了後、アプリをローカル起動して確認

```bash
cd finance-app
VITE_USE_MOCK=true npm run dev
```

ブラウザで `http://localhost:5173` を開いてダッシュボードが表示されることを確認

---

### Step C — Yahoo Finance データ層の実装

**【話す内容】**
```
「スキャフォールドはモックデータで動いています。
次は実際の Yahoo Finance データ連携を実装します。」
```

**【操作】** プロンプトを貼り付けて Enter

> **💬 プロンプトを貼り付ける前に話す内容**
> 「スキャフォールドはモックデータで動いています。次は Yahoo Finance のリアルデータをつなぎます。プロンプトのポイントは2つ——『正規化してください』と『後で企業を追加しやすく』という制約です。API の戻り値はバラバラなので、UI 側が形を意識しなくて済むようにアダプター層を作らせます。」

```
IBM とその主要な競合他社を対象に、Yahoo Finance データを使った
金融データ層を実装してください。

UI が株価サマリー・短期履歴・四半期トレンドビューを表示できるよう、
取得したデータを正規化してください。

後で企業を追加しやすいよう、コードは拡張しやすい設計にしてください。
```

---

### Step D — ダッシュボードの構築

**【話す内容】**
```
「データ層ができました。次にビジネス価値が見える
ダッシュボードビューを3つ構築します。」
```

**【操作】** プロンプトを貼り付けて Enter

> **💬 プロンプトを貼り付ける前に話す内容**
> 「データ層ができたので、今度は画面を作ります。3つのビューを箇条書きで指定して、あとは『シンプルで読みやすく、デモに適した UI に』と伝えるだけです。デザインの細かい指定はしていません。Bob に任せます。」

```
金融アプリ用に、以下のダッシュボードビューを2〜3個構築してください：

- 当日の市場サマリー
- 過去7日間のトレンド比較
- 前四半期の比較ビュー

IBM と最大4社の競合他社を含めてください。
チャートとサマリーカードを適切に組み合わせ、
シンプルで読みやすく、デモに適した UI にしてください。
```

**【確認ポイント】**
- Current Day・Last 7 Days・Last Quarter の3タブが機能する
- IBM・競合4社のデータが表示される

---

### Step E — テスト・バリデーション

**【話す内容】**
```
「AI が生成したコードでも、エンジニアリングの規律は必要です。
Bob にテストとバリデーションを追加させます。」
```

**【操作】** プロンプトを貼り付けて Enter

> **💬 プロンプトを貼り付ける前に話す内容**
> 「AI が生成したコードはそのままでは信頼できません。このプロンプトでは『データ変換のテスト』と『UI レンダリングのテスト』を最低1つずつ、と明示しています。加えて『ローカルでチェックを走らせるコマンドも出してください』と依頼することで、実行結果をその場で確認できます。」

```
金融ダッシュボードプロジェクトに適切なバリデーションを追加してください。
データ変換ロジックのテストと、少なくとも1つの UI レンダリングパスの
テストを作成してください。

アプリのチェックをローカルで実行するコマンドを提供し、
成功の状態がどのようなものかをまとめてください。
```

**【確認ポイント】**
- lint + test + build がすべてグリーンであることを確認
- テスト件数・ファイル数を Bob が報告する

---

### Step F — コミット・PR 説明文の生成

**【話す内容】**
```
「アプリが完成しました。
次に Bob にコミットメッセージと PR 説明文を生成させます。
Bob は diff を読んで、レビュアーに必要な情報をすべて盛り込みます。」
```

**【操作】** プロンプトを貼り付けて Enter

> **💬 プロンプトを貼り付ける前に話す内容**
> 「アプリが完成しました。このプロンプトでは、Bob に git の diff を読ませて『何を作ったか』をまとめさせ、コミットメッセージと PR の説明文まで出力させます。自分でゼロから書く必要がないのがポイントです。『不足している点も特定してください』と加えているので、見落としがあれば Bob が指摘してくれます。」

```
このリポジトリの未コミットの変更をレビューし、
金融ダッシュボードに追加されたものをまとめ、
不足している点を特定してください。

最初のアプリケーション納品に向けた、簡潔なコミットサマリーと
Pull Request の説明文を準備してください。
```

**【確認ポイント】**
- Bob がコミットメッセージと PR 説明文を HTML レポートとして出力する
- 内容を確認してコピーしておく（Part 2 で使用）

```
【話す内容】
「コードを書いただけでなく、
GitHub で協働するための成果物も Bob が準備してくれました。
これで Part 1 は完了です。次は GitHub への公開です。」
```

---

## Part 2 | Evolve

---

### Step G — GitHub リポジトリへの公開

**【話す内容】**
```
「ここからが GitHub との本格的な連携です。
Bob にリポジトリの状態を確認させ、
公開の準備を整えさせます。」
```

**【操作】** まず GitHub.com で空のリポジトリを作成（ブラウザで操作）
- Repository name: `finance-app`
- README・.gitignore・license は **追加しない**
- 作成後、表示されるリモートURLをコピー

**【操作】** Bob に確認させる

> **💬 プロンプトを貼り付ける前に話す内容**
> 「GitHub にプッシュする前に、Bob に『本当に準備できているか』を確認させます。`.gitignore` が足りない、node_modules がステージングに含まれている、といったブロッカーを Bob が検出して報告します。人間が見落としがちなチェックを、ここで Bob に任せます。」

```
現在のリポジトリの変更を検査し、金融ダッシュボードの
現在の実装状況をまとめ、プロジェクトが
コミットして GitHub にプッシュする準備ができているか確認してください。
```

**【操作】** ターミナルで順番に実行

```bash
# .gitignore を作成
cat > .gitignore <<'EOF'
node_modules/
dist/
.DS_Store
.env
coverage/
EOF

# git 初期化・リモート接続
git init
git remote add origin <YOUR_REMOTE_URL>
git remote -v

# ステージング・コミット・プッシュ
git add .
git status   # node_modules・dist が含まれていないことを確認！
git commit -m "<BobのStep Fで生成したコミットメッセージ>"
git branch -M main
git push -u origin main
```

**【操作】** フィーチャーブランチを作成してプッシュ

```bash
git checkout -b feature/initial-finance-dashboard
git push -u origin feature/initial-finance-dashboard
```

**【確認ポイント】**
- GitHub Actions タブに Finance App CI の実行が表示される
- ステータスが **Success** になっていることを確認

**【操作】** GitHub Issues タブで Issue を作成
- **タイトル:** `Add a graph for a user selected company`
- **本文:**
  ```
  ユーザーが指定した企業の新しいグラフを追加してください。

  * ダッシュボードが株式シンボルまたは企業の選択を受け付けるようにする
  * 選択した企業の追加比較チャートをレンダリングする
  * 無効またはサポートされていないシンボルは明確なメッセージをユーザーに表示する
  * IBM と競合他社の既存のダッシュボードは変更しないこと
  ```
- Issue 番号を確認（通常 #1）

---

### Step H — GitHub Issue から機能要件を分析

**【話す内容】**
```
「Issue が作成されました。
Bob に Issue を取得・分析させ、
コードを書く前に受け入れ基準を定義させます。
これが Part 1 と同じ 'plan-first' の規律です——
外部から来た要求にも同じアプローチを適用します。」
```

**【操作】** 機能ブランチを作成

```bash
git checkout -b feature/user-selected-company-chart
git push -u origin feature/user-selected-company-chart
```

**【操作】** Agent モードで以下のプロンプトを貼り付けて Enter

> **💬 プロンプトを貼り付ける前に話す内容**
> 「今度は GitHub Issue を起点にします。プロンプトの核心は『Issue を取得して、受け入れ基準を定義して、何を変えればいいか特定してください』という3段構成です。そして最後に『既存のダッシュボードを壊すな』という制約を明示しています。コードを書く前にスコープを合意する——Part 1 と同じ流れを、外部の Issue に対してもやります。」

```
ユーザーが選択した企業に基づいた新しいグラフを要求している
GitHub Issue または Pull Request のコンテキストを取得してください。

要求された機能強化を分析し、受け入れ基準を定義し、
ユーザー選択企業グラフを実装するために必要な
UI・状態・データサービスの変更を特定してください。

IBM と競合他社を比較する既存のダッシュボードを壊さずに
この機能を実装する方法も説明してください。
```

**【確認ポイント】**
- Bob が Feature Request Analysis レポートを生成する（**Open** で確認）
- 4つの要求から9つの受け入れ基準（AC-1〜AC-9）が導出されていることを確認
- 影響を受けないファイルも明示されていることを確認

```
【話す内容】
「4つの箇条書きの Issue から、
Bob は検証方法付きの9つの受け入れ基準を導き出しました。

重要なのは『何を変えないか』も明示していること。
既存の3つのダッシュボードは一切触らない——
これが確認できてから、初めて実装に進みます。」
```

---

### Step I — 機能実装

**【話す内容】**
```
「スコープが合意できました。Bob に実装を依頼します。」
```

**【操作】** プロンプトを貼り付けて Enter

> **💬 プロンプトを貼り付ける前に話す内容**
> 「スコープが合意できたので、実装を依頼します。ポイントは3つ——『既存のデータ層を再利用する』『既存のダッシュボードを保持する』『データがなければフォールバックせず明確なエラーを出す』。この制約を明示することで、Bob は既存コードを書き換えずに機能を追加します。」

```
金融ダッシュボードで、ユーザーが企業シンボルを入力すると
その企業の新しいグラフが表示される機能を実装してください。

可能な限り既存の金融データ層を再利用し、
UX はシンプルに保ち、既存のダッシュボードを保持してください。

シンボルのデータが利用できない場合は、
他の企業のデータにフォールバックするのではなく、
明確な「データなし」メッセージを表示してください。
```

**【確認ポイント】**
- Bob が依存関係の順序を考慮してバッチ単位で実装する
- 既存のコンポーネントが変更されていないことを確認

```
【話す内容】
「Bob は依存順序を考慮しながら5つのバッチに分けて実装しました。
新しいコンポーネント・フック・テストが追加され、
既存の3つのビューは1行も変更されていません。」
```

---

### Step J — バリデーション

**【話す内容】**
```
「実装が完了しました。Bob にバリデーションを実行させます。
ローカルで通過するだけでなく、GitHub Actions でも
同じチェックが走ることを後で確認します。」
```

**【操作】** プロンプトを貼り付けて Enter

> **💬 プロンプトを貼り付ける前に話す内容**
> 「実装が終わりました。このプロンプトは一言でいうと『全部テストして、GitHub にプッシュできる状態かどうか判定してください』です。Pass / Fail の結果だけでなく、『ブランチの準備ができているか』という最終判断まで Bob に出させます。」

```
ユーザーが選択した企業グラフの新機能に対して、
適切なプロジェクトバリデーションを実行してください。

何が通過し、何が失敗したか、
そしてブランチを GitHub に戻してプッシュする準備ができているかを
まとめてください。
```

**【確認ポイント】**
- ESLint 0 errors、全テスト PASS、クリーンビルドの3つがグリーン
- 新しい受け入れ基準がテストでカバーされていることを確認

**【操作】** ブラウザで動作確認

```bash
cd finance-app
npm run dev:mock
```

- `IBM1` を入力 → バリデーションエラーが表示されること（AC-2）
- `AAPL` を入力 → 「データが見つかりません」メッセージ（AC-4）
- `NVDA` を入力 → チャートとカードが表示されること（AC-5）
- タブを切り替えても Custom Company パネルが残ること（AC-8）

---

### Step K — PR の準備・レビュー・マージ

**【話す内容】**
```
「最後のステップです。Bob に差分をレビューさせ、
PR 説明文を生成させます。
マージの判断は人間が行います——
これがこのワークフロー全体の設計思想です。」
```

**【操作】** プロンプトを貼り付けて Enter

> **💬 プロンプトを貼り付ける前に話す内容**
> 「最後のプロンプトです。Bob に差分を読ませて、PR の説明文と push コマンドをまとめて出力させます。このプロンプト1つで『diff のレビュー・PR 説明文の生成・push 手順の提示』が同時に返ってきます。あとはコマンドをターミナルに貼り付けて、GitHub で PR を作るだけです。」

```
更新されたブランチをレビューし、機能の実装内容をまとめ、
Pull Request の更新説明文を生成し、
GitHub の Pull Request を完了するために変更をプッシュするための
git コマンドを提供してください。
```

**【操作】** Bob が生成したコマンドをターミナルで実行

```bash
git add -A
git commit -m "<Bobが生成したコミットメッセージ>"
git push -u origin feature/user-selected-company-chart
```

**【操作】** GitHub.com でプルリクエストを作成
- Compare & pull request をクリック
- Bob の PR 説明文を各セクションに貼り付け
- `Linked Issue` に `Closes #1` と記入
- **Create pull request** をクリック

**【確認ポイント】**
- GitHub Actions が自動で CI を実行する（Finance App CI #2）
- lint + test + build が全てグリーンになることを確認

**【操作】** Bob に最終確認させる

> **💬 プロンプトを貼り付ける前に話す内容**
> 「マージの前に、Bob に『このブランチは本当に問題ないか』を最終確認させます。コミット・ワーキングツリーのクリーンさ・テスト結果・受け入れ基準の充足を一覧で出力させます。これで人間が判断する材料が揃います。」

```
完了した機能強化をまとめ、ブランチの準備ができていることを確認し、
マージまたはリクエストのクローズに必要な最終的な GitHub アクションを説明してください。
```

**【操作】** GitHub で **Merge pull request** → **Confirm merge** をクリック

**【確認ポイント】**
- PR が `Merged` に変わる
- Issues タブで Issue #1 が自動的に `Closed` になっていることを確認

```
【話す内容】
「'Closes #1' が効いて、マージと同時に Issue が閉じました。

Issue を作って、Bob が読んで、コードを書いて、テストを通して、
CI が検証して、自分がマージする。
次の機能改善も同じ手順です。」
```

---

## クロージング（Stand & Deliver 用）

> 📢 **デモ画面のまま話し続けてください**

---

### ③ CTA（次のアクション）

```
【話す内容】

「Issue を起点に、Bob が分析して実装してテストして PR を準備する。
CI が独立した環境で検証して、自分がマージを押す。
それだけです。

今日お見せしたのは、既存の GitHub ワークフローをそのまま使いながら
Bob がその中に入り込む、という話でした。
ツールを変える必要はありません。

次のステップを2つ提案します。

1つ目は、御社の実際のリポジトリで同じことをやる1日のワークショップです。
今日のシナリオをそのまま持ち込んで試せます。

2つ目は、実際の Issue や PR に Bob を当てる PoX です。
実際の数字で効果を確認できます。

どちらでも、最初の1歩は小さいリポジトリで十分です。
興味があればあとで声をかけてください。」
```

---

## 付録：トラブルシューティング早見表

| 症状 | 対処 |
|---|---|
| Bob のレスポンスが予期と異なる | Bob は非決定論的。実装計画の確認時に方針を修正して再送する |
| `npm run dev` が起動しない | `cd finance-app` で正しいディレクトリにいるか確認 |
| ライブデータが表示されない | 502 エラーの場合はプロキシ未起動。`npm run proxy` を先に実行してから `VITE_USE_MOCK=false npm run dev` |
| CI が失敗する | Bob に「失敗したゲートを修正して再実行してください」と依頼 |
| `git push` で認証エラー | PAT（Personal Access Token）またはSSHキーを確認。`ssh -T git@github.ibm.com` で接続テスト |
| `git branch -M main` でエラー | 初回コミット後に実行する（コミット前は branch が存在しないためエラーになる） |
| Issue が自動クローズされない | PR 説明文の `Closes #1` が独立した行にあるか確認。なければ手動でクローズ |
| Bobcoin が不足している | トライアルアカウントを新規作成（40 Bobcoin 付与）するか、IBMer は内部登録を利用 |

---

## 参考資料

| ドキュメント | 場所 |
|---|---|
| ラボガイド（PDF） | `Github SDLC Automation/GitHub SDLC Automation with IBM Bob Level 3 [Bob SDLC L3].PDF` |
| 評価チェックリスト | `Github SDLC Automation/Evaluation Checklist - GitHub SDLC Automation with IBM Bob Level 3 [Bob SDLC L3].DOCX` |
| セットアップガイド | `Github SDLC Automation/SETUP-GUIDE-JA.md` |
| Part 1 ワークショップ | `Github SDLC Automation/WORKSHOP-part1-BUILD.md` |
| Part 2 ワークショップ | `Github SDLC Automation/WORKSHOP-part2-GITHUB-AUTOMATION.md` |
